; No `Roles` declaration means only the always-present Env role, equivalent to:
; (Roles)
; This is the default:
; (TimeUnit d)

(GlobalVars
	(n : Nat = 3)
)

(FormalContract "test for local vars"
	(StartSection Ongoing)

	(Section Ongoing
		(Next
			(DoIt immediately)
		)
	)

	(Action DoIt
		(StateTransform
			(local x : Nat = (n * n))
			(local y : Nat = (x * x))
		)
		(FollowingSection
			(Next
				((FinishIt y) immediately)
			)
		)
	)

	(Action (FinishIt (z : Nat))
		(StateTransform
			(n' = z)
		)
		(TransitionsTo Fulfilled)
	)
)